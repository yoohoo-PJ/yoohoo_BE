spring:
  config:
    activate:
      on-profile: team

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # TODO: 팀원 분의 로컬 DB 이름으로 맞춰주세요
    url: jdbc:mysql://localhost:3306/library_yoohoo_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    
    # TODO: 팀원 분의 로컬 DB 계정으로 맞춰주세요
    username: root
    password: "YOUR_DB_PASSWORD" 

  jpa:
    hibernate:
      ddl-auto: update # 또는 none
